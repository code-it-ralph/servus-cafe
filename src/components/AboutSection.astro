---
import siteConfig from '../data/siteConfig.json';
---

<section id="about" class="py-24 bg-white overflow-hidden">
  <div class="max-w-6xl mx-auto px-4">
    <div class="grid lg:grid-cols-2 gap-16 items-center">
      {/* Text Content */}
      <div class="order-2 lg:order-1">
        <div
          class="inline-block px-3 py-1 bg-coffee-100 text-coffee-600 text-xs font-bold uppercase tracking-widest rounded-full mb-6"
        >
          Our Story
        </div>
        <h2
          class="font-serif text-4xl md:text-5xl font-bold text-coffee-900 mb-8 leading-tight"
        >
          Come as a guest, <br />
          <span class="text-coffee-500 italic">leave as a friend.</span>
        </h2>
        <div class="space-y-6 text-coffee-800/80 leading-relaxed text-lg">
          <p>
            The name <span class="font-bold text-coffee-900">Servus</span> is more
            than just a brand—it's an ancient Latin greeting that translates to "at
            your service." It perfectly captures our philosophy of hospitality here
            in the heart of San Juan, Batangas.
          </p>
          <p>
            What started as a passion for the perfect brew has grown into a
            community hub. Whether you're a local resident starting your day or
            a traveler looking for a cozy stopover before hitting the beach at
            Laiya, we've designed our space to be your "third home."
          </p>
        </div>

        {/* Amenities Grid */}
        <div class="mt-12 grid grid-cols-2 gap-6">
          {
            siteConfig.amenities
              .filter((item) => item.available)
              .map((item) => (
                <div class="flex gap-4 p-4 rounded-2xl bg-coffee-50 border border-coffee-100/50 hover:border-coffee-200 transition-colors">
                  <div class="text-2xl">{item.icon}</div>
                  <div>
                    <h4 class="font-bold text-coffee-900 text-sm">
                      {item.label}
                    </h4>
                    <p class="text-[11px] text-coffee-500 leading-tight mt-0.5">
                      {item.description}
                    </p>
                  </div>
                </div>
              ))
          }
        </div>
      </div>

      {/* Image / Visual Side */}
      <div class="order-1 lg:order-2 relative">
        <div
          class="aspect-[4/5] rounded-3xl overflow-hidden shadow-2xl rotate-2 hover:rotate-0 transition-transform duration-500"
        >
          <img
            src="/logo.jpg"
            alt="Servus Cafe Interior"
            class="w-full h-full object-cover scale-110"
          />
        </div>
        {/* Decorative elements */}
        <div
          class="absolute -bottom-6 -left-6 bg-coffee-900 text-white p-8 rounded-2xl hidden md:block shadow-xl -rotate-3"
        >
          <p class="font-serif text-3xl font-bold italic">Est. 2023</p>
          <p
            class="text-xs uppercase tracking-widest opacity-60 mt-2 text-coffee-200"
          >
            Brewing Daily
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
